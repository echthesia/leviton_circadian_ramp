[project]
name = "leviton-circadian-ramp"
version = "0.2.0"
description = "Circadian brightness ramp for Matter-enabled dimmers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-matter-server>=7.0",
    "aiohttp>=3.9",
    "python-dotenv>=1.0",
]
